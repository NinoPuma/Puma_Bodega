import SwiftUI

// Vista de Envio
struct Envio: View {
    var body: some View {
        NavigationStack {
            Text("Página de Envío")
                .font(.largeTitle)
                .navigationTitle("Envío")
        }
    }
}
