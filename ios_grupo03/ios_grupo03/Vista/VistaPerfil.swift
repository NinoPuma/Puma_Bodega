import SwiftUI

// Vista de Perfil
struct VistaPerfil: View {
    var body: some View {
        NavigationStack {
            Text("Página de Perfil")
                .font(.largeTitle)
                .navigationTitle("Perfil")
        }
    }
}
